---

spring:
  application:
    name: attendee-viewer
  cloud:
    config:
      uri: ${vcap.services.cfgserverrepo.credentials.cfguri}   
    
management:
  security:
    enabled: false

feign:
  hystrix:
    enabled: true
